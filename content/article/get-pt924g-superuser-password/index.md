---
title: "（转载）获取中国移动PT924(G)等光猫的超级管理员密码"
date: 2025-11-23T18:15:00+08:00
draft: false
categories: ['Learning']
tags: ['Republish', 'Learning', '2025']
summary: "转载自：@FrostFox-霜狐"
authors:
  - "frostfox"
---

> 转载自：[获取中国移动PT924(G)等光猫的超级管理员密码-FrostFox](https://www.frostfox.cn/tittle-tattle/blog/2024/light-cat.html)

## 写在前面
为什么要转这一篇别人的博客文章呢？主要是原网页加了个“阅读剩余”的功能，然后不知道是原博客的 CDN 炸了还是咋了，一直看不到全文。再加上自己的（移动）运营商一直在改超级用户密码需要时不时走一遍流程。故扒下来一是方便自己做参考，二是偷偷做一份留存。

目前我使用的光猫为友华通讯生产的移动 PT928-R3（无语音），生产日期 2024年2月3日。如果有相近设备的可以试试以下方法。

以下为原博文正文：

-----

{{< alert >}}
警告&免责声明：本文中所有操作仅为方便疫情期间中国移动宽带师傅不便上门调试时临时使用，如被用于违法用途或造成损失，本文作者和霜狐的科技屋不负任何责任！
{{< /alert >}}

## 问题：

有时我们需要对光猫做一些更改。然而，往往通过光猫背面的Web用户登录密码是无法获取到太多的权限的。此时就需要获取光猫的超级管理员密码来实现对它管理了。

## 方法：

### 视频版：

[[光猫][移动]获取中国移动PT924(G)等光猫的超级管理员密码演示-FrostFox-霜狐](https://www.bilibili.com/video/BV15T4y1v73Y)

注意：本方法只适用于部分中国移动光猫，仅在PT924和PT924G上测试通过，其他型号暂时未知。

### 1.开启光猫的Telnet服务：

在浏览器中访问[http://192.168.1.1/cgi-bin/abcdidfope94e0934jiewru8ew414.cgi](http://192.168.1.1/cgi-bin/abcdidfope94e0934jiewru8ew414.cgi)，页面出现“success”即为成功。

### 2.安装Telnet客户端：

打开Win的控制面板，在程序与功能中找到“启用或关闭Windows功能”，勾选上“Telnet客户端”，稍等即可。

### 3.连接光猫：

Win+R打开运行，输入cmd，在弹出的命令提示符中输入以下内容并回车：
```bash
telnet 192.168.1.1
```

然后回车，弹出login时输入以下内容并回车：
```bash
yhtcAdmin
```

接着弹出Password，输入以下内容并回车：
```bash
Cm1@YHfw
```

注意：输入Password这一步时密码不会显示！

出现“#”即为成功。

### 4.获取超级管理员密码：

依次输入以下两行指令并回车：
```bash
cd /usr/local/ct
vi lastgood.xml
```

弹出的一堆文字即为成功。

然后打一个“:”，再输入以下指令并回车：
```bash
/SUSER_PASSWORD
```

会把你的光标自动定位到“SUSER_PASSWORD" Value="xxxxx" 这里，其中xxxxx指你的超级管理员密码，每个人不同。

### 5.登录：

此时打开`192.168.1.1`，在登录页面在用户账号处填写“CMCCAdmin”，密码处填写你刚刚获取到的超级管理员密码，然后便可登录管理了。

-----

## 写在后面

1. 如果访问开启 Telnet 的链接发现没用/跳转到登录页面，先登录普通权限的 user 账号，然后同一浏览器新标签页中访问该链接；

2. `/usr/local/ct/lastgood.xml` 不存在的话，就在同文件夹下找 `config.xml`，字段相同。